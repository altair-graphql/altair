type User {
  id: String!
  email: String!
  firstName: String!
  lastName: String
}

input CreateUserInput {
  email: String!
  firstName: String!
  lastName: String
}

type Query {
  getUserById(id: String!): User
}
